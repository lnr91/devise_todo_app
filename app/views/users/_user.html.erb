<li>
  <%= user.nick_name %>&emsp;
  <% if current_user.admin? && !current_user?(user)%>
      <%= link_to 'remove', user_path(user), method: :delete, data: {confirm: "You're sure ?"} %>
  <% end %>
</li>